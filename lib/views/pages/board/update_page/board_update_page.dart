import 'package:flutter/material.dart';

class BoardUpdatePage extends StatelessWidget {
  const BoardUpdatePage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
