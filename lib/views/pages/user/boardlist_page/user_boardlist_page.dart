import 'package:flutter/material.dart';

class UserBoardlistPage extends StatelessWidget {
  const UserBoardlistPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
